@model List<SharedObjects.ValueObjects.VProduct>
@if (Model != null && Model.Count() > 0)
{
    <div class="ltn__product-tab-content-inner">
        <div class="row ltn__tab-product-slider-one-active slick-arrow-1">
            <!-- ltn__product-item -->
            @foreach (var item in Model)
            {
                string url = $"{HostConfig.Domain}/images/product/{item.Avatar}";
                <div class="col-lg-12">
                    <div class="ltn__product-item ltn__product-item-3 text-center">
                        <div class="product-img">
                            <a asp-action="Detail" asp-controller="Product" asp-route-productId="@item.ProductId"><img width="266" height="266" src="@url" alt="#"></a>
                            <div class="product-badge">
                                @*<ul>
                        <li class="sale-badge">-19%</li>
                        </ul>*@
                            </div>
                            @*<div class="product-hover-action">
                    <ul>
                    <li>
                    <a class="view" href="#" title="Quick View" data-toggle="modal" data-target="#quick_view_modal" data-id="@item.ProductId">
                    <i class="far fa-eye"></i>
                    </a>
                    </li>
                    <li>
                    <a href="#" title="Add to Cart" data-toggle="modal" data-target="#add_to_cart_modal">
                    <i class="fas fa-shopping-cart"></i>
                    </a>
                    </li>
                    <li>
                    <a href="#" title="Wishlist" data-toggle="modal" data-target="#liton_wishlist_modal">
                    <i class="far fa-heart"></i>
                    </a>
                    </li>
                    </ul>
                    </div>*@
                        </div>
                        <div class="product-info">
                            <div class="product-ratting">
                                <ul>
                                    <li><a href="#"><i class="fas fa-star"></i></a></li>
                                    <li><a href="#"><i class="fas fa-star"></i></a></li>
                                    <li><a href="#"><i class="fas fa-star"></i></a></li>
                                    <li><a href="#"><i class="fas fa-star-half-alt"></i></a></li>
                                    <li><a href="#"><i class="far fa-star"></i></a></li>
                                    <li class="review-total"> <a href="#"> (24)</a></li>
                                </ul>
                            </div>
                            <h2 class="product-title"><a asp-action="Detail" asp-controller="Product" asp-route-productId="@item.ProductId">@item.ProductName</a></h2>
                            <div class="product-price">
                                <span>@item.Price.Value.ToString("#,##0") VNĐ</span>
                            </div>
                        </div>
                    </div>

                </div>
            }
        </div>
    </div>
}
else
{
    <h4 class="text-center"> Hiện chưa có sản phẩm nào</h4>

}